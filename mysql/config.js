const config = {
    // 启动端口
    port: 5000,

    // 数据库配置
    database: {
        DATABASE: 'micro_campus',
        USERNAME: 'root',
        PASSWORD: 'whk4302070',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config
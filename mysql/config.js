const config = {
    port: 5000,
    database: {
        DATABASE: 'micro_campus',
        USERNAME: 'root',
        PASSWORD: 'whk4302070',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config